# MCP Servers presentation

An opinionated presentation explaining what the Model Context Protocol is, how it works, where it helps, and why it is not required for agentic AI.

## View locally

Open `index.html` in a browser.

## GitHub Pages

GitHub Pages publishes the site automatically from the repository root on `main`.
